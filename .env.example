# Docker Compose 部署配置
TAG=v0.1.0
APP_PORT=8900
TZ=Asia/Shanghai
ADMIN_API_KEY=
ALLOW_PRIVATE_ENDPOINTS=true

# PostgreSQL 示例（默认使用容器内 SQLite）
# DATABASE_URL=postgresql://mineru:mineru_pass@postgres:5432/mineru

# 前端 API 地址（可选；不设置时使用相对路径 /api）
# VITE_API_BASE_URL=https://your-backend-api.com
