services:
  app:
    image: mineru-batch:${TAG:-latest}
    ports:
      - "8900:8900"
    volumes:
      - data:/data
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped

volumes:
  data:
