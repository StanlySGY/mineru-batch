services:
  app:
    image: mineru-batch:${TAG:?TAG not set — run ./docker-build.sh first}
    ports:
      - "8900:8900"
    volumes:
      - data:/data
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped

volumes:
  data:
